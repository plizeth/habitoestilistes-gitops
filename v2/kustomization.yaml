apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: habito-web

resources:
  - configmap.yaml
  - deployment.yaml
  - service-route.yaml
  # namespace.yaml eliminado — ArgoCD lo gestiona con CreateNamespace=true
